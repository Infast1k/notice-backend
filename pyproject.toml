[project]
name = "notice"
version = "0.0.1"
description = "Backend for markdown notes taking application"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = [
    "pre-commit>=4.6.0",
    "ruff>=0.15.20",
]
